services:
  litellm:
    image: ghcr.io/berriai/litellm:v1.83.7-stable
    container_name: litellm-proxy
    volumes:
      - ./litellm-config.yaml:/app/config.yaml
    ports:
      - "127.0.0.1:${LITELLM_PORT:-4000}:4000"
    environment:
      - UI_USERNAME=${UI_USERNAME:-admin}
      - UI_PASSWORD=${UI_PASSWORD:-secure_admin_password_123}
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY:-sk-local-agent-secure-12345}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-llmproxy}:${POSTGRES_PASSWORD:-dbpassword9090}@db:5432/${POSTGRES_DB:-litellm}
      - STORE_MODEL_IN_DB=True
      - OLLAMA_HOST=${OLLAMA_HOST:-http://host.docker.internal:11434}
      - OLLAMA_MODEL=${OLLAMA_MODEL:-qwen2.5-coder:14b}
      - LITELLM_OLLAMA_MODEL=ollama/${OLLAMA_MODEL:-qwen2.5-coder:14b}
    command: ["--config", "/app/config.yaml", "--port", "4000"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request, sys; sys.exit(0) if urllib.request.urlopen('http://localhost:4000/health/readiness').getcode() == 200 else sys.exit(1)\""]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16.13
    container_name: litellm-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-llmproxy}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-dbpassword9090}
      - POSTGRES_DB=${POSTGRES_DB:-litellm}
    volumes:
      - litellm_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-llmproxy} -d ${POSTGRES_DB:-litellm}"]
      interval: 5s
      timeout: 5s
      retries: 5

  ngrok:
    image: ngrok/ngrok:3.37.6-alpine
    container_name: ngrok-tunnel
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    command: ["http", "litellm:4000"]
    ports:
      - "127.0.0.1:4040:4040"
    depends_on:
      litellm:
        condition: service_healthy

volumes:
  litellm_data:
